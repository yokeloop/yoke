---
name: gca
description: Создание атомарного коммита с автоматической привязкой к тикету
---

# Коммит с привязкой к тикету

Создать атомарный коммит. Определить ticket ID, сгенерировать conventional commit message, закоммитить.

---

## Шаг 1 — Сбор контекста

Выполни параллельно:

```bash
git status
```

```bash
git diff HEAD
```

```bash
git diff --stat HEAD
```

```bash
git ls-files --others --exclude-standard
```

Если нет изменений (ни staged, ни unstaged, ни untracked) — сообщи пользователю и останови.

---

## Шаг 2 — Определение ticket ID

Каскад приоритетов:

### 2a. Из аргументов

`$ARGUMENTS` может содержать ticket ID или URL. Правила извлечения:

| Вход                                      | Ticket ID  |
| ----------------------------------------- | ---------- |
| `86` или `#86`                            | `#86`      |
| `R2-50`                                   | `R2-50`    |
| `https://github.com/.../issues/86`        | `#86`      |
| URL YouTrack содержащий `/issue/PROJ-123` | `PROJ-123` |

Если `$ARGUMENTS` пуст или не содержит ticket — перейти к 2b.

### 2b. Из имени ветки

```bash
git branch --show-current
```

Паттерны:

| Имя ветки               | Regex          | Ticket ID  |
| ----------------------- | -------------- | ---------- |
| `86-feature-name`       | `^(\d+)-`      | `#86`      |
| `feature/86-name`       | `\/(\d+)-`     | `#86`      |
| `R2-50-feature`         | `(R\d+-\d+)`   | `R2-50`    |
| `fix/R2-50-name`        | `(R\d+-\d+)`   | `R2-50`    |
| `PROJ-123-feature`      | `([A-Z]+-\d+)` | `PROJ-123` |
| `main`, `develop`, etc. | —              | не найден  |

Если ticket найден — использовать. Если нет — перейти к 2c.

### 2c. Спросить пользователя

Через AskUserQuestion предложить варианты:

- **Без тикета** — закоммитить без привязки
- **Ввести номер** — пользователь вводит свой ticket ID

---

## Шаг 3 — Стейджинг

Добавить все изменённые и новые файлы. **НЕ стейджить:**

- `.env`, `.env.*` — секреты
- Файлы с credentials, tokens, keys
- Большие бинарные файлы

Если обнаружены такие файлы в untracked — предупредить пользователя перед стейджингом.

Стейджить конкретные файлы по именам (не `git add -A`).

---

## Шаг 4 — Commit message

Формат:

```
[ticket] <type>(<scope>): <описание>
```

**ticket** — первым элементом. `#86` для GitHub, `R2-50` / `PROJ-123` для YouTrack. Без тикета — пропустить.

**type** — определить по характеру изменений:

| Тип        | Когда                               |
| ---------- | ----------------------------------- |
| `feat`     | Новая функциональность              |
| `fix`      | Исправление бага                    |
| `refactor` | Рефакторинг без изменения поведения |
| `docs`     | Только документация                 |
| `test`     | Только тесты                        |
| `chore`    | Сборка, CI, зависимости, конфиги    |
| `perf`     | Оптимизация производительности      |

**scope** — опционально, основная область изменений.

**описание** — одно предложение, активный залог, на языке проекта.

Примеры:

- `#86 feat(pages): добавить страницу блэкджека`
- `R2-50 fix: исправить сохранение user ID в БД`
- `chore: обновить зависимости`

---

## Шаг 5 — Коммит

```bash
git commit -m "<message>"
```

Без `Co-Authored-By`, `Signed-off-by` и любых trailer lines.

После коммита — показать результат: хэш, сообщение, список файлов.

---

## Правила

- Один коммит — одно логическое изменение
- Ticket ID всегда первым в сообщении (если есть)
- Не использовать `wip`, `temp`, `misc`
- Не коммитить секреты и credentials
- Сообщение коммита — краткое, конкретное, активный залог
